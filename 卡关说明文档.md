# 卡关挑战游戏完整说明文档

## 1. 项目定位

本项目的目标，不是单纯做一个“能左右移动、能跳、能过刺”的 Pygame 小游戏，而是做成一个更像课程大作业的完整关卡作品。

这次改造重点放在四件事上：

1. 把游戏流程做完整：菜单、闯关、暂停、死亡、过关切换、最终胜利。
2. 把关卡逻辑做复杂：动态平台、假墙、塌陷平台、事件触发、封门遭遇战、分路与奖励路线。
3. 把 UI 做清晰：HUD 与可玩区域彻底分离，提示信息更像正式作品。
4. 把设计思路做明确：整体节奏参考《超级马力欧》的教学 -> 变化 -> 组合 -> 考试。

---

## 2. 设计思路

### 2.1 为什么参考《超级马力欧》

这类平台游戏最重要的不是把障碍堆得很密，而是让玩家感到“关卡是被设计过的”。

因此本项目参考了马力欧常见的几条设计原则：

- 机制先教学，再变化，再组合，最后考试
- 危险要有视觉预告，而不是凭空恶心玩家
- 关卡不只是一条死路，最好有主路线和奖励路线
- 检查点不仅是降低挫败，也是在切分节奏
- 终点前应该有一个明显的收尾高潮段

### 2.2 本项目如何落地这些原则

本项目将三关分别设计成不同功能：

- 第一关：教学关，重点让玩家理解动态平台、塌陷平台、假墙
- 第二关：路线与联动关，重点是上下分路、联动机关和封门战
- 第三关：综合考试关，把前两关机制组合到一起

---

## 3. 系统设计

## 3.1 游戏状态机

`main.py` 中实现了完整的状态机：

- `menu`：菜单
- `playing`：游玩中
- `paused`：暂停
- `dead`：死亡
- `transition`：过关切换
- `victory`：全部通关后的结算

这样的结构让项目不再只是一个死循环 demo，而是完整的可交互应用。

## 3.2 数据驱动关卡系统

关卡内容统一写在 `levels.json` 中，加载由 `level_loader.py` 负责。

当前支持的元素包括：

- 普通地面平台 `wall`
- 动态平台 `dynamic_platform`
- 塌陷平台 `falling_platform`
- 假墙 `fake_wall`
- 机关门 / 显隐桥 `toggle_wall`
- 收集物 `token`
- 尖刺 `hazard`
- 检查点 `checkpoint`
- 触发区域 `trigger`
- 终点区 `exit_zone`

这意味着后续要扩展关卡，不需要改主循环，只需要继续编辑 JSON 数据。

## 3.3 事件系统

这是本项目最像“大作业”的部分之一。

当前事件系统支持：

- `set_hint`：显示阶段提示
- `drop_platform`：触发塌陷平台
- `break_fake_wall`：让假墙消失
- `toggle_wall`：开门、关门、显桥、隐藏踏板
- `spawn_enemy` / `spawn_wave`：刷怪
- `start_encounter`：启动遭遇战
- `encounter_wave`：遭遇战中的增援波次

其中 `start_encounter` 支持：

- 进入区域后锁门
- 按波次刷怪
- 清敌后解锁
- 同时显现新桥梁或新路线

---

## 4. 实现的主要机制

## 4.1 动态平台

动态平台支持水平或垂直移动，角色站在上面时会跟随平台位移，避免出现“平台在动但玩家原地掉下去”的问题。

用途：

- 教学节奏
- 中段路线变化
- 终点前高压连续跳

## 4.2 塌陷平台

塌陷平台采用“踩上后延迟坠落，过一段时间再重生”的方式实现。

它的作用不是单纯坑人，而是让玩家做节奏判断：

- 要不要立刻起跳
- 要不要先观察
- 这块平台是过渡点还是假安全区

## 4.3 假墙与隐藏路线

假墙在被事件触发后会淡出消失，同时可能出现隐藏踏板或奖励路线。

它承担两个作用：

- 提供“发现感”
- 让关卡不止一条线

## 4.4 机关门与桥梁

机关门本质上是可显隐墙体。

它可以用于：

- 封门战
- 解锁新路
- 显示桥梁
- 控制节奏切换

## 4.5 遭遇战系统

第二关和第三关都加入了阶段式遭遇战。

逻辑流程：

1. 玩家进入触发区
2. 前后门关闭
3. 敌人刷新
4. 经过一段时间追加增援波次
5. 清完全部敌人后解锁出口并显现桥梁

这会让关卡从“只会跑酷”升级成“阶段挑战”。

---

## 5. 三关关卡设计说明

## 5.1 第一关：机关热身场

### 设计目标

- 教会玩家读懂平台运动方向
- 认识塌陷平台和假墙
- 通过一个小规模封门战完成第一关收尾

### 关卡结构

1. 开场教学区：安全地面 + 第一组移动平台
2. 中段变化区：竖直移动平台 + 塌陷平台
3. 假墙奖励区：触发后显现隐藏踏板与收集物
4. 收尾考试段：终点前封门战 + 增援波次

### 设计亮点

- 不会一上来就把所有机制堆满
- 玩家能在前段认识规则，再在后段接受组合考核

## 5.2 第二关：联动试炼井

### 设计目标

- 强化墙跳、滑翔和下落路线控制
- 引入主路线 / 奖励路线
- 让机关触发影响后续路径

### 关卡结构

1. 竖井起步区：升降平台与墙跳教学
2. 分路区：上路更快，下路更稳
3. 假墙奖励段：触发后出现上层奖励踏板
4. 遭遇战区：封门 + 两轮增援
5. 终点冲刺区：桥梁显现后进入连续跳跃

### 设计亮点

- 玩家会明显感觉到“我走的是一条被设计过的路线”
- 事件不再只是提示，而是实打实改变地图结构

## 5.3 第三关：终局机关考场

### 设计目标

- 让玩家综合运用前两关学到的一切
- 用高机制密度制造终局感
- 终点前形成一个完整收尾高潮

### 关卡结构

1. 前段：动态平台 + 裂纹地板 + 假路线
2. 中段：隐藏补给踏板与中继检查点
3. 核心遭遇战：锁门、刷怪、增援、解桥
4. 后段：桥梁、塌陷平台、头顶刺、终点前爬升
5. 终点：旗杆式终点区

### 设计亮点

- 不是单纯加刺，而是机制密度更高
- 玩家通关后会有明显“考试通过”的完成感

---

## 6. UI 与交互优化

## 6.1 HUD 与游戏区分离

原先 UI 容易压进地板区域，影响游玩和观感。

现在采用：

- 顶部固定 HUD
- 下方完整可玩视区
- 帮助面板悬浮在左上，不再挡住底部地形

## 6.2 HUD 内容结构

顶部 HUD 现在分为四块：

1. 关卡信息：第几关、关卡名、副标题
2. 状态信息：用时、死亡数、目标时间
3. 进度信息：得分、检查点、生命值、阶段提示
4. 提示信息：当前建议、事件触发数

同时加入：

- 整关进度条
- 左上角 `H` 帮助切换提示
- 帮助面板

## 6.3 中央提示与结算

游戏中间还会出现：

- 阶段提示
- 过关横幅
- 死亡提示
- 暂停面板
- 最终胜利结算

这让作品更像一个完整产品，而不是只有底层逻辑。

---

## 7. 代码结构说明

## 7.1 `classes.py`

负责实体外观和基础行为，包含：

- `Wall`
- `DynamicPlatform`
- `FallingPlatform`
- `FakeWall`
- `ToggleWall`
- `Token`
- `Hazard`
- `TriggerZone`
- `Checkpoint`
- `ExitZone`

## 7.2 `main.py`

负责：

- 状态机
- HUD
- 事件调度
- 遭遇战
- 波次增援
- 摄像机更新
- 关卡切换

## 7.3 `Player.py`

负责玩家动作系统：

- 移动
- 跳跃
- 二段跳
- 墙跳
- 滑翔
- 射击
- 受伤击退
- 敌人与尖刺碰撞

## 7.4 `Enemy.py`

负责敌人行为：

- 平台巡逻
- 边缘回头
- 被踩死
- 被子弹击退与击杀

---

## 8. 运行与验证

## 8.1 运行方式

在项目目录中执行：

```bash
python main.py
```

如果使用固定环境：

```bash
D:\it_study\Anaconda\envs\pygame_env\python.exe main.py
```

## 8.2 编译验证

执行：

```bash
D:\it_study\Anaconda\envs\pygame_env\python.exe -m py_compile main.py Player.py Enemy.py Camera.py classes.py level_loader.py Projectile.py settings.py
```

结果：通过。

## 8.3 冒烟测试

执行：

```bash
D:\it_study\Anaconda\envs\pygame_env\python.exe main.py --smoke-test 180
```

结果：通过，输出：

```text
冒烟测试通过
```

---

## 9. 适合作业展示的亮点总结

这份项目现在比较适合拿来做 Python 大作业展示，原因主要有这些：

- 不是单关地图，而是完整三关流程
- 使用数据驱动方式组织关卡
- 具备多类型平台和事件系统
- 有动态路线变化、隐藏路线和奖励收集
- 有阶段式遭遇战和波次增援
- UI 与游戏区域分离，完成度更高
- 设计逻辑明确，可以讲清楚“为什么这样设计”

---

## 10. 后续还能继续扩展什么

如果还想继续打磨，后续最值得加的是：

1. 角色帧动画与音效
2. 图块资源与更完整美术主题
3. 通关评分、S/A/B 等级与排行榜
4. 更多事件类型，例如落石、移动尖刺、追逐型机关
5. 关卡选择界面与存档
